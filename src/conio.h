#ifndef CONIO_H
#define CONIO_H

/*
	DISCLAIMER: conio.h is not my code. I needed to borrow it from the Linux 'ncurses' library,
	as the conio library is only available on Windows.
*/

/* reads from keypress, doesn't echo */
int getch(void);

#endif
